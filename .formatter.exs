[
  plugins: [Phoenix.LiveView.HTMLFormatter],
  inputs: [
    "*.{ex,exs,heex}",
    "{config,lib,test}/**/*.{ex,exs,heex}",
    "apps/*/{config,lib,test}/**/*.{ex,exs,heex}"
  ],
  line_length: 100
]
